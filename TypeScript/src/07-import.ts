// Importamos el type UserExport
import { UserExport } from './08-export';

// Generamos una constante de tipo UserExport
const newUserHere: UserExport = { user: 'John', id: 1 };

// Se muestra en pantalla
console.log(newUserHere);
